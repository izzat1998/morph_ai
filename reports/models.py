"""
Models for the reports app.

Currently, this app uses the existing Cell and CellAnalysis models
from the cells app and doesn't define its own models.
"""

# No custom models needed for this app
# We use the existing models from cells.models:
# - Cell
# - CellAnalysis 
# - DetectedCell